spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: off
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/study_distibuted_system_id
    username: root
    password: password
    hikari:
      maximum-pool-size: 10

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE